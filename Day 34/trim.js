//trim() remove the extra spaces
//trimStart() remove the extra spaces from the start
//trimEnd() remove the extra spaces from the end
let str = "     Dahle     Coder     Boss     ";
console.log(str.trim());
console.log(str.trimStart());
console.log(str.trimEnd());
